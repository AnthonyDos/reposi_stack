package com.pastrycertified.pastrycertified.api.src.utils;

/**
 * Enumération représentant les types de jetons (tokens) dans le système.
 * Le seul type de jeton actuellement supporté est le {@code BEARER}.
 */
public enum TokenType {
    /**
     * Type de jeton Bearer.
     */
    BEARER
}

